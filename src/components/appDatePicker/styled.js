import styled from "styled-components";
import { AppPickerButton } from "../appPickerButton";

export const Button = styled(AppPickerButton)`
  width: 100%;
`;

export const Wrapper = styled.div`
  margin-bottom: 15px;
`;

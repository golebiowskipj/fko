import styled from "styled-components";

export const AppPickerHeaderStyled = styled.h2`
  font-size: ${({ theme }) => theme.fontL};
  font-family: ${({ theme }) => theme.fontFamilySecondary};
  margin-bottom: 10px;
`;
